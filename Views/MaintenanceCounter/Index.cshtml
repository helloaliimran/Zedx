@model IEnumerable<Zedx.Models.MaintenanceCounter>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ColumnName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TableName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Counter)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ColumnName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TableName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Counter)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaintenanceCounterId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaintenanceCounterId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaintenanceCounterId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
